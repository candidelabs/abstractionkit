{
  "name": "CreateAndSendTransaction",
  "version": "1.0.0",
  "description": "create a new account and send a useroperation and pay gas with erc20 token using a paymaster.",
  "main": "dist/index.js",
  "scripts": {
    "build": "rimraf dist && tsc",
    "clean": "rimraf dist",
    "format": "prettier --write src/**/*.ts",
    "lint": "eslint src/**/*.ts"
  },
  "license": "MIT",
  "dependencies": {
    "dotenv": "^16.3.1",
    "ethers": "^6.6.7",
    "typescript": "^5.1.6",
    "abstractionkit": "^0.2.4"
  },
  "devDependencies": {
    "rimraf": "^5.0.10"
  }
}
