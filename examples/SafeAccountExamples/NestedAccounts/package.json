{
  "name": "NestedAccounts",
  "version": "1.0.0",
  "description": "An example demonstrating nested safe multisigs using 4337.",
  "main": "dist/index.js",
  "scripts": {
    "build": "rimraf dist && tsc",
    "clean": "rimraf dist",
    "format": "prettier --write src/**/*.ts",
    "lint": "eslint src/**/*.ts"
  },
  "license": "MIT",
  "dependencies": {
    "dotenv": "^16.3.1",
    "ethers": "^6.6.7",
    "typescript": "^5.1.6",
    "abstractionkit": "^0.2.17"
  },
  "devDependencies": {
    "rimraf": "^5.0.10"
  }
}
