{
  "name": "WebAuthnCreateAndSendTransaction",
  "version": "1.0.0",
  "description": "create a new account and send a useroperation.",
  "main": "dist/index.js",
  "scripts": {
    "build": "rimraf dist && tsc",
    "clean": "rimraf dist",
    "format": "prettier --write src/**/*.ts",
    "lint": "eslint src/**/*.ts"
  },
  "license": "MIT",
  "dependencies": {
    "cbor": "^9.0.2",
    "dotenv": "^16.3.1",
    "ethers": "^6.11.0",
    "typescript": "^5.1.6",
    "abstractionkit": "^0.2.16"
  },
  "devDependencies": {
    "rimraf": "^5.0.10"
  }
}
