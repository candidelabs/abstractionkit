{
  "name": "CreateAndSendGaslessTx",
  "version": "1.0.0",
  "description": "Create a new account and send sponsor the fees for the useroperation on behaf of the account using a paymaster.",
  "main": "dist/index.js",
  "scripts": {
    "build": "rm -rf dist && tsc",
    "clean": "rm -rf dist",
    "format": "prettier --write src/**/*.ts",
    "lint": "eslint src/**/*.ts"
  },
  "license": "MIT",
  "dependencies": {
    "abstractionkit": "0.0.9",
    "dotenv": "^16.3.1",
    "ethers": "^6.6.7",
    "typescript": "^5.1.6"
  }
}
